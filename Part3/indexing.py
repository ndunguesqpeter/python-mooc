#The program loops thro all characters in a string from first to last
#Initialisation
#The program loops thro all characters in a string from first to last
#Initialisation
input_string = input("Please type in a string: ")
index = 0
#Condition
while index < len(input_string):
    print(f"{input_string[index]} : {index}")
    #Update variables
    index += 1